import React from 'react'

const Badge = ({ children }) => (
  <span className="inline-block px-2 py-1 text-xs font-semibold rounded bg-blue-100 text-blue-800">{children}</span>
)

export default Badge
